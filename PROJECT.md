# Project Context

## Open Questions

- PR #2 (Sepo v0.2.0→v0.3.0) is open as of 2026-06-01 — introduces `AGENT_ENABLED` pause guard and `AGENT_MODEL_POLICY` per-route model selection; README.md needs `AGENT_ENABLED=false` mention added (line 71) before PR test suite passes 100%.
