# Memory

## Durable
- Set repo var AGENT_ENABLED=false to pause all Sepo agent workflows (added in v0.3.0)
- Set repo var AGENT_MODEL_POLICY to configure per-route model selection across all Sepo workflows (added in v0.3.0)
